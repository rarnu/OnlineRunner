用数据库保存latex图片文件的md5+sha1值，并对应该值的latex和error，以便后续直接查询，而不是再经过api请求

需要有一个工具处理编译时的异常信息，过滤路径

HTML编辑器

代码编辑器

Latex编辑器

编辑器只能传入具体size,不支持百分比，编辑器内每一个组件都需要被保存以便修改size
在最外层可以用onresize来监听大小的改变，并且调用编辑器的 resize 方法进行更改。


